console.log(a)
var a="aryan"

console.log(b)
let b ="anshika"

// console.log(a);   // Output: undefined
// // `var a` is hoisted. Declaration is moved to the top, but assignment happens later. So `a` exists but is undefined.

// var a = "aryan";

// console.log(b);   // ReferenceError: Cannot access 'b' before initialization
// // `let b` is hoisted but in Temporal Dead Zone. Accessing it before declaration throws ReferenceError.

// let b = "anshika";
